# Copia este archivo a .env y ajusta variables según tu entorno
# HOST=0.0.0.0 expone el backend en la red local (revisar firewall). Usar 127.0.0.1 para solo local.
HOST=127.0.0.1
PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/cafe_gourmet
JWT_SECRET=pon-un-secreto-fuerte-aqui
